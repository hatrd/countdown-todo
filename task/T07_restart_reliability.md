# T07 - 重启恢复与可靠性验收

- 状态：DONE
- 负责人：Agent B
- 依赖：T02, T03, T05, T06

## 目标
验证程序退出/重启后 mark 链条连续，确保长期任务不中断。

## 输入
- `PRD.md` 第 4.2(约束3) / 8.2(条目4) / 10(条目6)

## 输出
- 重启恢复测试用例
- 故障场景清单
- 验收记录模板

## DoD
1. 重启前后连续 mark 时，`prev_marked_at` 指向重启前最后一条 mark。
2. CSV 异常场景有错误提示和恢复建议。
3. 回归测试覆盖至少：空数据、单条、多条、跨 timer。
